source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in zeus-service-engine.gemspec.
gemspec

gem 'rack-cors'
gem 'faraday'
gem 'zeus_sdk', '>= 0.5.5'
gem 'administrate'

group :development do
  gem 'sqlite3'
end

# To use a debugger
# gem 'byebug', group: [:development, :test]
